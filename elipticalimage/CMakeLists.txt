cmake_minimum_required(VERSION 2.8)
project( elipticalimage )
find_package( OpenCV REQUIRED )
include_directories(${OpenCV_INCLUDE_DIRS} )
add_executable( elipticalimage elipticalimage.cpp )
target_link_libraries(elipticalimage ${OpenCV_LIBS} )
